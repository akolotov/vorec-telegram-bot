# Telegram bot token obtained from @BotFather.
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Unique Docker Compose project name for this deployment.
COMPOSE_PROJECT_NAME=vorec-telegram-bot

# Defaults to the published latest image and always pulls before startup.
# For a local build, set VOREC_BOT_IMAGE=vorec-telegram-bot:local and
# VOREC_BOT_PULL_POLICY=never.
# VOREC_BOT_IMAGE=ghcr.io/akolotov/vorec-telegram-bot:latest
# VOREC_BOT_PULL_POLICY=always

# The public Tailscale Funnel hostname and route handled by tailscale-ingress.
WEBHOOK_PUBLIC_BASE_URL=https://wabelfish-funnel.taild8e94b.ts.net
WEBHOOK_DOCKER_ALIAS=vorec-telegram-bot
WEBHOOK_PATH=/hooks/vorec-telegram-bot/telegram/webhook
# Generate a random 1-256 character value using A-Z, a-z, 0-9, _ or -.
WEBHOOK_SECRET_TOKEN=replace_with_a_long_random_webhook_secret
# WEBHOOK_LISTEN=0.0.0.0
# WEBHOOK_PORT=8080

# Comma-separated Telegram user IDs allowed to use the bot.
ALLOWED_USER_IDS=123456789,987654321

# Native GigaAM server on the Mac host. Use a long random token.
GIGAAM_URL=http://host.docker.internal:18000/v1
GIGAAM_API_KEY=replace_with_a_long_random_token
# GIGAAM_MODEL=whisper-1
# Defaults to ~/.omlx/models/ai-babai/gigaam-multilingual-mlx when omitted.
# GIGAAM_MODEL_PATH=/Users/your-user/.omlx/models/ai-babai/gigaam-multilingual-mlx
# GIGAAM_PORT=18000
# GIGAAM_VARIANT=int8
# GIGAAM_TMUX_SESSION=vorec-gigaam
# An optional absolute path to the checkout that owns the shared GigaAM process.
# Set this in secondary deployments to reuse that checkout's GigaAM manager.
# GIGAAM_MANAGER_SCRIPT=/Users/your-user/services/vorec-telegram-bot/scripts/manage-gigaam.sh

# Any OpenAI-compatible endpoint used for the secondary transcription and merge.
# This project currently uses a local oMLX server, but a remote compatible provider works too.
# Uses bundled ffmpeg by default. Set to afconvert to use the macOS converter instead.
# AUDIO_CONVERTER=ffmpeg
INFERENCE_API_URL=http://host.docker.internal:8111/v1
INFERENCE_API_KEY=your_inference_api_key
# PRIMARY_TRANSCRIPTION_MODEL=whisper-large-v3-turbo-asr-fp16
# MERGE_MODEL=gemma-4-26b-a4b-it-4bit
